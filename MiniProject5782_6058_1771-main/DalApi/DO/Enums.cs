using System;

namespace DO
{
    /// <summary>
    /// different options for each of those specific type of object (weight,status and priorites)
    /// </summary>
    public enum WeightCategories { Low, Middle, Heavy };
    public enum Priorities { Regular, Fast, Emergency };
}